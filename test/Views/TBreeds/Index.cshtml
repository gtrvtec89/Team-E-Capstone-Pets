@model IEnumerable<test.TBreed>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.strBreedName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.strBreedName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.intBreedID }) |
            @Html.ActionLink("Details", "Details", new { id=item.intBreedID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.intBreedID })
        </td>
    </tr>
}

</table>
