@model IEnumerable<test.TPetType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.strPetType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.strPetType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.intPetTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.intPetTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.intPetTypeID })
        </td>
    </tr>
}

</table>
