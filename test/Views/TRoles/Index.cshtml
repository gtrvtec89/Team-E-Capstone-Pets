@model IEnumerable<test.TRole>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.strRoleName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.strRoleName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.intRoleID }) |
            @Html.ActionLink("Details", "Details", new { id=item.intRoleID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.intRoleID })
        </td>
    </tr>
}

</table>
